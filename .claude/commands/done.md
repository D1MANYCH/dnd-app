---
description: Закрыть фазу X-N в memory (обновить статус → done)
argument-hint: <X-N>
---
Аргумент: $ARGUMENTS. Найди соответствующий project_*_plan.md в `~/.claude/projects/.../memory/`, замени `**открыта**` на `**done**` для фазы $ARGUMENTS (только в её строке заголовка). Покажи diff. Напомни мне сделать коммит вручную (если ещё не сделан) с сообщением в стиле `тип(scope): $ARGUMENTS — описание`.
